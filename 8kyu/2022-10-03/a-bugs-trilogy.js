function yourFutureCareer() { // a simple function declaration without parameters.
    var career = Math.random(); // initializes a variable and assigns it a random number
      if (career <= 0.32)
        return 'FrontEnd Developer'
      else if (career <= 0.65) 
        return 'BackEnd Developer'
      else return 'Full-Stack Developer'
      }
  
  
  yourFutureCareer(); // this is the function call